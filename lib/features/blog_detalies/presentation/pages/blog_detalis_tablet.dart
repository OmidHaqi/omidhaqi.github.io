import 'package:flutter/material.dart';

class BlogDetalisTablet extends StatelessWidget {
  const BlogDetalisTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BlogDetalisTablet'),
      ),
    );
  }
}