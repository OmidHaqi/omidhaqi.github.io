import 'package:flutter/material.dart';

class BlogDetalisMobile extends StatelessWidget {
  const BlogDetalisMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BlogDetalisMobile'),
      ),
    );
  }
}